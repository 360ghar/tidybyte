{
  "name": "tidybyte-site",
  "private": true,
  "version": "1.0.0",
  "description": "Marketing site for TidyByte iOS app",
  "scripts": {
    "build": "tailwindcss -i ./src/input.css -o ./site/assets/css/styles.css --minify && node scripts/sync-chrome.mjs --check && node scripts/hash-assets.mjs",
    "dev": "tailwindcss -i ./src/input.css -o ./site/assets/css/styles.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
